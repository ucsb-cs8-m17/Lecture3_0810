import turtle

t = turtle.Turtle()

# Invoke the shape method,
#  passing the string "turtle"
#  as a parameter

t.shape("turtle") 

# A simple T

t.forward (50)
t.goto (25, 0)
t.right (90)
t.forward (100)
